# Fortigate Credentials
# API Token (Required).
# If set, this takes precedence (and is now the only method).
# API Token (Required).
# If set, this takes precedence (and is now the only method).
FORTIGATE_API_TOKEN=

# Per-Device Secrets:
# 1. By Name (Recommended): Use "FORTIGATE_API_TOKEN_" + Device Name (spaces to underscores).
#    Example: Device "Forti-Lab" -> Env Var "FORTIGATE_API_TOKEN_Forti-Lab"
# 2. By IP (Fallback): FORTIGATE_API_TOKEN_<IP_WITH_UNDERSCORES>
#    Example: FORTIGATE_API_TOKEN_192_168_1_1=xxxxx

# Management port (Default: 443)
FGT_PORT=33443
# Connection protocol (https or http)
FGT_PROTOCOL=https

# Encryption Settings
# Set to 'yes' to encrypt the downloaded backup file.
ENCRYPT_BACKUP=yes
#
# Required if ENCRYPT_BACKUP=yes. The password to decrypt the file.
BACKUP_ENCRYPTION_KEY=

# Storage Configuration
# Local path on the HOST where backups will be saved
HOST_BACKUP_DIR=./backups

# Automation Settings
# 'manual': Runs once and exits. 'schedule': Loops forever running daily.
RUN_MODE=manual
# If RUN_MODE=schedule, time to run backup (24h format, HH:MM)
# Automation Settings
# 'manual': Runs once and exits. 'schedule': Loops forever running daily.
RUN_MODE=manual
# If RUN_MODE=schedule, time to run backup (24h format, HH:MM)
SCHEDULE_TIME=03:00

# Debug Logging (yes/no)
DEBUG=no

# Web Interface (FileBrowser)
# To ENABLE the Web UI, set this to 'web'. To DISABLE, leave it empty.
COMPOSE_PROFILES=web

# Default credentials on first run: admin / admin
# Database is stored in Docker Volume 'forti_backup_filebrowser_data'

# Docker Network Configuration
# Specify a subnet to avoid conflicts with your network (e.g. 172.20.x.x)
# Recommended: 10.244.0.0/24
DOCKER_SUBNET=10.244.0.0/24

# Backup Retention (Local) -> Default: 30 days
LOGROTATE_DAYS=30

# Git Auto-Push Configuration (Optional)
# If provided, backups will be committed and pushed to this repo.
# GIT_PUSH_TOKEN (Personal Access Token) can be injected via Secrets.
GIT_REPO_URL=
GIT_PUSH_TOKEN=
GIT_USER_NAME="FortiBackup Bot"
GIT_USER_EMAIL="bot@backup.local"
